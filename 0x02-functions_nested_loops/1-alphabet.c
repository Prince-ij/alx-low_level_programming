#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Description: blah blah blah
 * Return: Always 0
 */
void print_alphabet(void)
{
	int a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
}
int main(void)
{
	return (0);
}
